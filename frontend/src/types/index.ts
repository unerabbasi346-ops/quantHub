// Governing specification: Doc 08 — Frontend Architecture (QH-008 v1.0)
// Shared TypeScript types used across feature modules — Doc 08 §API Layer
// Per Doc 00 §14.11

// UUID type alias — Doc 11 §canonical type system: uuid
export type UUID = string

// ISO 8601 timestamp — Doc 11 §canonical type system: timestamp
export type ISOTimestamp = string
